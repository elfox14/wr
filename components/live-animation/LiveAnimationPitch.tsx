'use client';

import { useEffect, useMemo, useRef, useState } from 'react';

type TeamVisualTheme = { code: string; flagEmoji: string; primaryColor: string; secondaryColor: string; accentColor: string; crowdPrimary: string; crowdSecondary: string; shirtPrimary: string; shirtSecondary: string };
type Team = { id: string; name: string; code?: string | null; image?: string | null; flagUrl?: string | null; theme?: TeamVisualTheme };
type AnimationEvent = { id: string; sequenceNumber: number; minute: number | null; second: number | null; teamId: string | null; playerId: string | null; playerName: string | null; jerseyNumber: string | null; eventType: string; eventLabel: string; detail: string; x: number; y: number; endX: number | null; endY: number | null; zone: string | null; coordinateSource?: 'EXACT_PROVIDER' | 'INFERRED_ZONE' | 'HEURISTIC' | string; coordinateConfidence?: 'HIGH' | 'MEDIUM' | 'LOW' | string; eventSide?: 'HOME_ATTACK' | 'AWAY_ATTACK' | 'NEUTRAL' | string; isInferred?: boolean; anchorZone?: string | null; displayPriority?: number; provider: string; icon: string; color: string; createdAt: string };
type MetricView = { key: string; label: string; home: number | null; away: number | null; suffix?: string; source: string; available: boolean };
type MatchClock = { label: string; phaseLabel: string; minute: number | null; source: string; verifiedStarted: boolean; verifiedFinished: boolean };
type AnimationState = { ok: boolean; mode: string; matchId: string; title: string; phase: 'scheduled' | 'live' | 'halftime' | 'finished' | string; status: string; minute: number | null; clock?: MatchClock; score: { home: number; away: number }; teams: { home: Team; away: Team }; visualTheme?: { home: TeamVisualTheme; away: TeamVisualTheme }; stats?: MetricView[]; lastSequence: number; events: AnimationEvent[]; source: string; lastUpdatedAt: string };

const ar = new Intl.NumberFormat('ar-EG');
const fallbackTheme: TeamVisualTheme = { code: 'TEAM', flagEmoji: '🏳️', primaryColor: '#E5E7EB', secondaryColor: '#94A3B8', accentColor: '#18E58F', crowdPrimary: '#E5E7EB', crowdSecondary: '#94A3B8', shirtPrimary: '#E5E7EB', shirtSecondary: '#111827' };

function teamTheme(team: Team): TeamVisualTheme { return team.theme || fallbackTheme; }
function teamImage(team?: Team | null) { return team?.flagUrl || team?.image || null; }
function fmt(value: number | null | undefined, suffix = '') { if (value === null || value === undefined || Number.isNaN(Number(value))) return '—'; const n = Number(value); return `${Number.isInteger(n) ? ar.format(n) : n.toLocaleString('ar-EG', { maximumFractionDigits: 2 })}${suffix}`; }
function TeamFlagImage({ team, className = 'h-5 w-7 rounded object-cover' }: { team?: Team | null; className?: string }) { const src = teamImage(team); if (!src) return <span className="inline-flex h-5 w-7 shrink-0 items-center justify-center rounded border border-white/10 bg-black/40 text-[9px] font-black text-[#F8C846]">{team?.code || '—'}</span>; return <img src={src} alt={`علم ${team?.name || 'منتخب'}`} className={`shrink-0 border border-white/10 ${className}`} loading="lazy" />; }
function phaseLabel(phase: string) { if (phase === 'live') return 'مباشر'; if (phase === 'halftime') return 'استراحة'; if (phase === 'finished') return 'انتهت'; return 'قبل المباراة'; }
function confidenceLabel(value?: string) { if (value === 'HIGH') return 'دقة عالية'; if (value === 'MEDIUM') return 'تقدير منطقي'; return 'تقدير بصري'; }
function sourceLabel(value?: string) { if (value === 'EXACT_PROVIDER') return 'إحداثيات حقيقية'; if (value === 'INFERRED_ZONE') return 'منطقة مستنتجة'; return 'تقدير بصري'; }
function eventTone(type: string) { const key = String(type || '').toLowerCase(); if (key.includes('goal')) return 'border-[#F8C846]/30 bg-[#F8C846]/10'; if (key.includes('red')) return 'border-red-400/30 bg-red-500/10'; if (key.includes('yellow')) return 'border-[#F8C846]/25 bg-[#F8C846]/10'; if (key.includes('sub')) return 'border-sky-300/25 bg-sky-400/10'; return 'border-[#18E58F]/25 bg-[#18E58F]/10'; }
function eventKey(event: AnimationEvent) { return [event.minute ?? '', String(event.eventType || '').toLowerCase(), event.teamId || '', String(event.playerName || '').toLowerCase(), String(event.detail || '').toLowerCase().slice(0, 90)].join('|'); }
function mergeEvents(prev: AnimationEvent[], incoming: AnimationEvent[]) { const rows = [...prev, ...incoming]; const seen = new Set<string>(); return rows.filter((event) => { const key = eventKey(event); if (seen.has(key)) return false; seen.add(key); return true; }).sort((a, b) => Number(a.sequenceNumber || 0) - Number(b.sequenceNumber || 0)).slice(-180); }
function statusBadgeClass(phase: string) { if (phase === 'finished') return 'border-sky-300/30 bg-sky-300/10 text-sky-100'; if (phase === 'halftime') return 'border-[#F8C846]/30 bg-[#F8C846]/10 text-[#F8C846]'; if (phase === 'live') return 'border-red-400/30 bg-red-400/10 text-red-100'; return 'border-white/10 bg-white/10 text-slate-200'; }
function metricWidth(home: number | null, away: number | null) { const h = Math.max(0, Number(home || 0)); const a = Math.max(0, Number(away || 0)); const total = h + a; if (!total) return { home: 50, away: 50 }; const hw = Math.max(6, Math.min(94, (h / total) * 100)); return { home: hw, away: 100 - hw }; }

function ActiveEventBar({ event, teams, isPlaying }: { event: AnimationEvent | null; teams: AnimationState['teams']; isPlaying: boolean }) {
  if (!event) return <div className="rounded-[1.5rem] border border-white/10 bg-white/[0.04] px-4 py-3 text-sm font-bold text-slate-400">لا يوجد حدث محدد بعد.</div>;
  const team = event.teamId === teams.home.id ? teams.home : event.teamId === teams.away.id ? teams.away : null;
  const theme = team ? teamTheme(team) : fallbackTheme;
  return <div className={`rounded-[1.5rem] border px-4 py-3 shadow-xl ${eventTone(event.eventType)}`}><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex min-w-0 items-center gap-3"><span className="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl border border-white/20 bg-black/40 text-3xl" style={{ boxShadow: `0 0 22px ${event.color}55` }}>{event.icon}</span><div className="min-w-0"><p className="flex flex-wrap items-center gap-2 text-xs font-black text-slate-300"><span>{isPlaying ? 'تشغيل تلقائي' : 'حدث محدد'} · {event.minute !== null ? `${event.minute}'` : '—'} · {team?.name || 'حدث عام'}</span>{team ? <TeamFlagImage team={team} className="h-4 w-6 rounded object-cover" /> : null}</p><h2 className="team-name-full text-2xl font-black text-white">{event.eventLabel}</h2><p className="team-name-full text-sm font-bold text-slate-200">{event.playerName ? `${event.playerName}${event.jerseyNumber ? ` #${event.jerseyNumber}` : ''}` : event.detail}</p></div></div><div className="flex flex-wrap gap-2 text-[10px] font-black"><span className="rounded-full border border-white/10 bg-black/25 px-2 py-1 text-slate-200">{event.provider}</span><span className="rounded-full border border-white/10 bg-black/25 px-2 py-1 text-slate-200">{sourceLabel(event.coordinateSource)}</span><span className="rounded-full border border-white/10 bg-black/25 px-2 py-1 text-slate-200">{confidenceLabel(event.coordinateConfidence)}</span>{event.eventType.includes('goal') && <span className="rounded-full px-2 py-1 text-black" style={{ backgroundColor: theme.crowdPrimary }}>تفاعل جماهير {team?.name || 'الفريق'}</span>}</div></div></div>;
}

function VirtualPitch({ state, activeEvent, isPlaying }: { state: AnimationState; activeEvent: AnimationEvent | null; isPlaying: boolean }) {
  const ballX = activeEvent?.endX ?? activeEvent?.x ?? 50;
  const ballY = activeEvent?.endY ?? activeEvent?.y ?? 50;
  const homeTheme = teamTheme(state.teams.home);
  const awayTheme = teamTheme(state.teams.away);
  return <div className="relative aspect-[16/11] overflow-hidden rounded-[2rem] border border-white/10 bg-[#06110d] p-3 shadow-2xl shadow-black/40 md:aspect-[16/10]"><div className="absolute inset-0 opacity-30" style={{ background: `linear-gradient(90deg, ${homeTheme.primaryColor}22, transparent 45%, transparent 55%, ${awayTheme.primaryColor}22)` }} /><div className="relative h-full overflow-hidden rounded-[1.5rem] border-2 border-white/20 bg-[#0a3b25]"><div className="absolute inset-4 rounded-[1.2rem] border-2 border-white/25" /><div className="absolute inset-y-4 left-1/2 w-0.5 bg-white/25" /><div className="absolute left-1/2 top-1/2 h-28 w-28 -translate-x-1/2 -translate-y-1/2 rounded-full border-2 border-white/25" /><div className="absolute left-4 top-[31%] h-[38%] w-[14%] rounded-r-2xl border-2 border-white/25" /><div className="absolute right-4 top-[31%] h-[38%] w-[14%] rounded-l-2xl border-2 border-white/25" />{activeEvent?.endX !== null && activeEvent?.endY !== null && activeEvent && <svg className="pointer-events-none absolute inset-0 z-20 h-full w-full" viewBox="0 0 100 100" preserveAspectRatio="none"><line x1={activeEvent.x} y1={activeEvent.y} x2={activeEvent.endX || activeEvent.x} y2={activeEvent.endY || activeEvent.y} stroke={activeEvent.color} strokeWidth="0.65" strokeDasharray="2 1.5" /></svg>}{activeEvent && <div className="absolute z-30 -translate-x-1/2 -translate-y-1/2 transition-all duration-700" style={{ left: `${activeEvent.x}%`, top: `${activeEvent.y}%` }}><div className={`flex h-12 w-12 items-center justify-center rounded-full border border-white/50 bg-black/80 text-2xl shadow-2xl ${isPlaying ? 'animate-pulse' : ''}`} style={{ boxShadow: `0 0 34px ${activeEvent.color}88` }}>{activeEvent.icon}</div></div>}<div className="absolute z-40 -translate-x-1/2 -translate-y-1/2 transition-all duration-700" style={{ left: `${ballX}%`, top: `${ballY}%` }}><span className="block h-5 w-5 rounded-full border border-white bg-white shadow-[0_0_18px_rgba(255,255,255,.75)]" /></div></div></div>;
}

function ClockPanel({ state }: { state: AnimationState }) { const clock = state.clock; return <div className="rounded-[1.5rem] border border-white/10 bg-black/30 p-3"><div className="flex flex-wrap items-center justify-between gap-2"><span className={`rounded-full border px-3 py-1 text-xs font-black ${statusBadgeClass(state.phase)}`}>{clock?.label || phaseLabel(state.phase)}</span><span className="rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs font-black text-slate-300">{clock?.source || 'Database state'}</span></div><div className="mt-3 grid grid-cols-3 gap-2 text-center text-[11px] font-black text-slate-300"><span className="rounded-xl bg-white/[0.04] p-2">بدء مؤكد: {clock?.verifiedStarted ? 'نعم' : 'لا'}</span><span className="rounded-xl bg-white/[0.04] p-2">الشوط: {clock?.phaseLabel || phaseLabel(state.phase)}</span><span className="rounded-xl bg-white/[0.04] p-2">نهاية مؤكدة: {clock?.verifiedFinished ? 'نعم' : 'لا'}</span></div></div>; }
function StatMetricCard({ metric, teams }: { metric: MetricView; teams: AnimationState['teams'] }) { const width = metricWidth(metric.home, metric.away); return <article className="rounded-2xl border border-white/10 bg-black/25 p-3"><div className="grid grid-cols-[56px_1fr_56px] items-center gap-2 text-center"><b className="text-sm font-black text-[#F8C846]">{fmt(metric.home, metric.suffix)}</b><div><p className="text-xs font-black text-white">{metric.label}</p><p className="mt-0.5 text-[9px] font-bold text-slate-500">{metric.source}</p></div><b className="text-sm font-black text-[#18E58F]">{fmt(metric.away, metric.suffix)}</b></div><div className="mt-2 flex items-center gap-2" dir="ltr"><div className="flex h-1.5 flex-1 justify-end overflow-hidden rounded-full bg-white/10"><span className="h-full bg-[#18E58F]" style={{ width: `${width.away}%` }} /></div><div className="flex h-1.5 flex-1 justify-start overflow-hidden rounded-full bg-white/10"><span className="h-full bg-[#F8C846]" style={{ width: `${width.home}%` }} /></div></div><div className="mt-2 grid grid-cols-2 text-[9px] font-bold text-slate-500"><span>{teams.home.name}</span><span className="text-left">{teams.away.name}</span></div></article>; }
function StatsPanel({ state }: { state: AnimationState }) { const stats = state.stats || []; return <div className="rounded-[2rem] border border-white/10 bg-white/[0.04] p-4"><div className="mb-3 flex flex-wrap items-center justify-between gap-2"><h2 className="text-xl font-black">إحصائيات البث التفاعلي</h2><span className="rounded-full border border-white/10 bg-black/25 px-3 py-1 text-xs font-black text-slate-300">TheStats + iSports Animation</span></div>{stats.length ? <div className="grid gap-2 sm:grid-cols-2">{stats.map((metric) => <StatMetricCard key={metric.key} metric={metric} teams={state.teams} />)}</div> : <div className="rounded-2xl border border-dashed border-white/15 p-5 text-center text-sm font-bold text-slate-400">لا توجد إحصائيات محفوظة بعد.</div>}</div>; }
function PlaybackButton({ isPlaying, disabled, onClick }: { isPlaying: boolean; disabled: boolean; onClick: () => void }) { return <button type="button" disabled={disabled} onClick={onClick} className={`rounded-full border px-3 py-1 text-xs font-black transition ${isPlaying ? 'border-red-400/30 bg-red-400/10 text-red-200 hover:bg-red-400 hover:text-black' : 'border-[#18E58F]/30 bg-[#18E58F]/10 text-[#18E58F] hover:bg-[#18E58F] hover:text-black'} disabled:cursor-not-allowed disabled:opacity-50`}>{isPlaying ? 'إيقاف الأحداث' : 'تشغيل الأحداث'}</button>; }

export default function LiveAnimationPitch({ initialState }: { initialState: AnimationState }) {
  const [state, setState] = useState<AnimationState>(initialState);
  const [events, setEvents] = useState<AnimationEvent[]>(mergeEvents([], initialState.events || []));
  const [activeIndex, setActiveIndex] = useState(Math.max(0, (initialState.events || []).length - 1));
  const [isPlaying, setIsPlaying] = useState(false);
  const lastSeqRef = useRef(initialState.lastSequence || 0);
  const activeEvent = useMemo(() => events[activeIndex] || events[events.length - 1] || null, [events, activeIndex]);
  const shouldPoll = state.phase === 'live' || state.phase === 'halftime';
  useEffect(() => { if (!events.length) { setIsPlaying(false); setActiveIndex(0); return; } if (activeIndex > events.length - 1) setActiveIndex(events.length - 1); }, [activeIndex, events.length]);
  useEffect(() => { if (!isPlaying || events.length <= 1) return; const timer = window.setInterval(() => { setActiveIndex((current) => (current >= events.length - 1 ? 0 : current + 1)); }, 2600); return () => window.clearInterval(timer); }, [events.length, isPlaying]);
  useEffect(() => { if (!shouldPoll) return; const timer = window.setInterval(async () => { try { const res = await fetch(`/api/matches/${state.matchId}/animation-state?afterSeq=${lastSeqRef.current}&limit=40`, { cache: 'no-store' }); const payload = await res.json(); if (!payload?.ok) return; setState(payload); if (payload.phase === 'finished' || String(payload.source || '').includes('TheStats')) { const nextEvents = mergeEvents([], payload.events || []); setEvents(nextEvents); lastSeqRef.current = Math.max(lastSeqRef.current, Number(payload.lastSequence || 0)); setActiveIndex(Math.max(0, nextEvents.length - 1)); return; } if (payload.events?.length) { setEvents((prev) => { const next = mergeEvents(prev, payload.events); if (!isPlaying) setActiveIndex(Math.max(0, next.length - 1)); return next; }); lastSeqRef.current = Math.max(lastSeqRef.current, Number(payload.lastSequence || 0)); } } catch {} }, 5000); return () => window.clearInterval(timer); }, [isPlaying, shouldPoll, state.matchId]);
  function togglePlayback() { if (!events.length) return; setIsPlaying((current) => { if (!current && activeIndex >= events.length - 1) setActiveIndex(0); return !current; }); }
  function selectEvent(index: number) { setIsPlaying(false); setActiveIndex(index); }
  return <div className="grid gap-4 lg:grid-cols-[1fr_390px]"><section className="space-y-3"><div className="rounded-[2rem] border border-white/10 bg-white/[0.04] p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-2xl font-black md:text-3xl">الملعب التفاعلي المباشر</h1><p className="mt-1 text-sm font-bold text-slate-400">{state.title} · {phaseLabel(state.phase)} · مصدر الأحداث: {state.source}</p><div className="mt-3 flex flex-wrap gap-2 text-xs font-black"><span className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-black/25 px-3 py-1"><TeamFlagImage team={state.teams.home} />{state.teams.home.name}</span><span className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-black/25 px-3 py-1"><TeamFlagImage team={state.teams.away} />{state.teams.away.name}</span></div></div><div className="rounded-2xl border border-white/10 bg-black/30 px-5 py-3 text-3xl font-black">{state.score.home} - {state.score.away}</div></div><div className="mt-3"><ClockPanel state={state} /></div></div><ActiveEventBar event={activeEvent} teams={state.teams} isPlaying={isPlaying} /><VirtualPitch state={{ ...state, events }} activeEvent={activeEvent} isPlaying={isPlaying} /><StatsPanel state={state} /></section><aside className="rounded-[2rem] border border-white/10 bg-white/[0.04] p-4"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><h2 className="text-xl font-black">الأحداث</h2><div className="flex items-center gap-2"><PlaybackButton isPlaying={isPlaying} disabled={!events.length} onClick={togglePlayback} /><span className="rounded-full border border-white/10 bg-black/25 px-3 py-1 text-xs font-black text-slate-300">{events.length} حدث</span></div></div><div className="mb-3 rounded-2xl border border-white/10 bg-black/25 px-3 py-2 text-xs font-bold leading-6 text-slate-400">أثناء المباراة من iSports Animation. بعد نهاية المباراة من TheStats فقط لمنع التكرار.</div><div className="max-h-[72vh] space-y-3 overflow-y-auto pr-1">{events.slice().reverse().map((event, reverseIndex) => { const originalIndex = events.length - reverseIndex - 1; const active = originalIndex === activeIndex; const team = event.teamId === state.teams.home.id ? state.teams.home : event.teamId === state.teams.away.id ? state.teams.away : null; return <button key={`${event.id}-${reverseIndex}`} onClick={() => selectEvent(originalIndex)} className={`w-full rounded-2xl border p-3 text-right transition ${active ? 'border-[#18E58F]/40 bg-[#18E58F]/10' : 'border-white/10 bg-black/20 hover:bg-white/[0.06]'}`}><div className="flex items-start gap-3"><span className="text-2xl">{event.icon}</span><div><p className="flex flex-wrap items-center gap-2 text-xs font-black text-slate-400">{event.minute !== null ? `${event.minute}'` : '—'} · {event.eventLabel}{team ? <TeamFlagImage team={team} className="h-4 w-6 rounded object-cover" /> : null}</p><h3 className="mt-1 font-black text-white">{event.playerName ? `${event.playerName}${event.jerseyNumber ? ` #${event.jerseyNumber}` : ''}` : event.detail}</h3><p className="mt-1 text-xs font-bold text-slate-500">{event.provider} · {sourceLabel(event.coordinateSource)} · {confidenceLabel(event.coordinateConfidence)}</p></div></div></button>; })}{!events.length && <div className="rounded-2xl border border-dashed border-white/15 p-5 text-center text-sm font-bold text-slate-400">لا توجد أحداث محفوظة بعد.</div>}</div></aside></div>;
}
